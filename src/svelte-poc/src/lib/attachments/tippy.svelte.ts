// import tippy, { delegate, type Content } from "tippy.js";

// export function tooltip({ content, interactive = false }: { content: Content; interactive?: boolean }) {
//   return (node: HTMLElement) => {
//     const tooltip = tippy(node, { content, interactive, theme: 'idea-catalog' });
//     return tooltip.destroy;
//   };
// }

// export function tippyParent(selector: string = '*') {
//   return (node: HTMLElement) => {
//     const instance = delegate(node, { target: selector });
//     return instance.destroy;
//   };
// }